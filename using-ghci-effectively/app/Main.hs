module Main (main) where

-- import Lib

-- my variant uses Emacs REPL
-- after loading the file

-- tab completion works
-- :commads work
-- :set +s enables function timing
-- multiline experiment in repl did not work with :{ }:
-- but multiline with Ctrl-j works

main :: IO ()
main = putStrLn "Using GHCi effectively"
